---
number: 3206
title: "`autoUpdate` not called consistently if an ancestor has `container-type: size/inline-size` with `contain: layout` workaround"
type: bug
state: open
created: 2025-01-27
url: "https://github.com/floating-ui/floating-ui/issues/3206"
reactions: 3
comments: 0
labels: "[bug]"
---

# `autoUpdate` not called consistently if an ancestor has `container-type: size/inline-size` with `contain: layout` workaround

**Describe the bug** 

We've run into a scenario involving https://github.com/floating-ui/floating-ui/issues/3067 where `autoUpdate` is not called consistently after the layout is updated.

**To Reproduce**



Steps to reproduce the behavior:

...